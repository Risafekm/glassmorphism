// ignore_for_file: file_names

import 'package:flutter/material.dart';

/// TabBar Decoration

BoxDecoration tabDecoration = BoxDecoration(
    color: const Color.fromARGB(255, 10, 71, 121).withOpacity(1),
    border: Border.all(color: Colors.blue.shade100),
    borderRadius: BorderRadius.circular(30));

///TabBar Selected Label color

const kselectedTabColor = Colors.white;

///TabBar UnSelected Label color

const kunselectedTabColor = Colors.white;

///TabBar indicator color

const ktransperant = Colors.transparent;
